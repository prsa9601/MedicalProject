@page
@using MedicalProject.Infrastructure
@using MedicalProject.Infrastructure.EnumUtil
@using MedicalProject.Infrastructure.Utils
@using MedicalProject.Models.User.Enum
@model MedicalProject.Pages.Admin.Users.IndexModel
@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>مدیریت کاربران - پلتفرم دنگ</title>

    <!-- فونت‌ها -->
    <link href="https://fonts.googleapis.com/css2?family=Vazir:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/gh/rastikerdar/vazir-font@v30.1.0/dist/font-face.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">


    <!-- SweetAlert2 -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">

    <!-- jQuery اول (اگر استفاده می‌کنی) -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <!-- سپس SweetAlert -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>


    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        /* استایل‌های سفارشی */
        html {
            direction: rtl;
            font-family: 'Vazir', 'IRANSans', system-ui, sans-serif;
        }

        body {
            background-color: #f9fafb;
            color: #111827;
            font-family: 'Vazir', 'IRANSans', system-ui, sans-serif;
        }

        * {
            border-color: #e5e7eb;
        }

        input:focus, textarea:focus, select:focus {
            outline: none;
            ring: 2px;
            ring-color: #3b82f6;
            border-color: #3b82f6;
        }

        /* کامپوننت‌های سفارشی */
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 0.5rem 1rem;
            border: 1px solid transparent;
            font-size: 0.875rem;
            font-weight: 500;
            border-radius: 0.5rem;
            box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
            transition: all 0.2s;
            cursor: pointer;
        }

            .btn:focus {
                outline: 2px solid transparent;
                outline-offset: 2px;
                ring: 2px;
                ring-offset: 2px;
            }

            .btn:disabled {
                opacity: 0.5;
                cursor: not-allowed;
            }

        .btn-primary {
            background-color: #2563eb;
            color: white;
        }

            .btn-primary:hover {
                background-color: #1d4ed8;
            }

        .btn-secondary {
            background-color: white;
            color: #374151;
            border: 1px solid #d1d5db;
        }

            .btn-secondary:hover {
                background-color: #f9fafb;
            }

        .btn-success {
            background-color: #059669;
            color: white;
        }

            .btn-success:hover {
                background-color: #047857;
            }

        .btn-danger {
            background-color: #dc2626;
            color: white;
        }

            .btn-danger:hover {
                background-color: #b91c1c;
            }

        .btn-outline {
            border: 1px solid #d1d5db;
            color: #374151;
            background-color: white;
        }

            .btn-outline:hover {
                background-color: #f9fafb;
            }

        .btn-warning {
            background-color: #d97706;
            color: white;
        }

            .btn-warning:hover {
                background-color: #b45309;
            }

        .card {
            background-color: white;
            border-radius: 0.75rem;
            box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
            border: 1px solid #f3f4f6;
            overflow: hidden;
        }

        .card-hover {
            transition: all 0.3s;
        }

            .card-hover:hover {
                box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
                transform: translateY(-0.25rem);
            }

        .input {
            display: block;
            width: 100%;
            padding: 0.5rem 0.75rem;
            border: 1px solid #d1d5db;
            border-radius: 0.5rem;
            box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
        }

            .input::placeholder {
                color: #9ca3af;
            }

            .input:focus {
                outline: none;
                ring: 2px;
                ring-color: #3b82f6;
                border-color: #3b82f6;
            }

        .label {
            display: block;
            font-size: 0.875rem;
            font-weight: 500;
            color: #374151;
            margin-bottom: 0.25rem;
        }

        .badge {
            display: inline-flex;
            align-items: center;
            padding: 0.25rem 0.625rem;
            border-radius: 9999px;
            font-size: 0.75rem;
            font-weight: 500;
        }

        .badge-primary {
            background-color: #dbeafe;
            color: #1e40af;
        }

        .badge-success {
            background-color: #d1fae5;
            color: #065f46;
        }

        .badge-warning {
            background-color: #fef3c7;
            color: #92400e;
        }

        .badge-danger {
            background-color: #fee2e2;
            color: #991b1b;
        }

        .badge-secondary {
            background-color: #f3f4f6;
            color: #6b7280;
        }

        .badge-info {
            background-color: #d1f5f9;
            color: #0e7490;
        }

        .text-gradient {
            background: linear-gradient(to right, #2563eb, #1e40af);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .admin-main {
            margin-right: 260px;
            transition: all 0.3s;
        }

        .admin-content {
            min-height: calc(100vh - 64px);
        }



        .table-responsive {
            overflow-x: auto;
        }

        .table {
            width: 100%;
            border-collapse: collapse;
        }

            .table th, .table td {
                padding: 0.75rem 1rem;
                text-align: right;
                border-bottom: 1px solid #e5e7eb;
            }

            .table th {
                background-color: #f9fafb;
                font-weight: 600;
                color: #374151;
            }

            .table tbody tr:hover {
                background-color: #f9fafb;
            }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: #374151;
        }

        .form-control {
            display: block;
            width: 100%;
            padding: 0.5rem 0.75rem;
            border: 1px solid #d1d5db;
            border-radius: 0.5rem;
            box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
            transition: all 0.2s;
        }

            .form-control:focus {
                outline: none;
                box-shadow: 0 0 0 2px #3b82f6;
                border-color: #3b82f6;
            }

        .form-text {
            display: block;
            margin-top: 0.25rem;
            font-size: 0.875rem;
            color: #6b7280;
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            object-fit: cover;
        }

        .status-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            display: inline-block;
            margin-left: 0.5rem;
        }

        .status-active {
            background-color: #10b981;
        }

        .status-inactive {
            background-color: #ef4444;
        }

        .status-pending {
            background-color: #f59e0b;
        }

        /* Responsive */
        @@media (max-width: 1024px) {
          
            .admin-main {
                margin-right: 70px;
            }

        }

        @@media (max-width: 768px) {
       

            .admin-main {
                margin-right: 0;
            }

        }







        .admin-main {
            margin-right: 260px;
            transition: all 0.3s ease-in-out;
            width: 100%;
        }

        .admin-content {
            min-height: calc(100vh - 64px);
        }


        @@keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        /* Responsive برای تبلت‌ها */
        @@media (max-width: 1024px) {
       
            .admin-main {
                margin-right: 70px;
            }

      
        }

        /* Responsive برای موبایل‌ها */
        @@media (max-width: 768px) {
        

            .admin-main {
                margin-right: 0;
                width: 100%;
                transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            }

         
        }

        /* برای موبایل‌های کوچکتر */
        @@media (max-width: 480px) {
     
        }


    </style>

    <style>
        :root {
            --sidebar-width: 260px;
            --sidebar-collapsed: 70px;
        }

        html {
            direction: rtl;
            font-family: 'Vazir', 'IRANSans', system-ui, sans-serif;
        }

        body {
            margin: 0;
            padding: 0;
            overflow-x: hidden;
        }

        /* سایدبار دسکتاپ */
        .admin-sidebar {
            width: var(--sidebar-width);
            background: linear-gradient(180deg, #1e40af 0%, #1e3a8a 100%);
            color: white;
            transition: all 0.3s ease;
            height: 100vh;
            position: fixed;
            right: 0;
            top: 0;
            z-index: 100;
            overflow-y: auto;
            box-shadow: 2px 0 10px rgba(0, 0, 0, 0.1);
        }

        .admin-main {
            margin-right: var(--sidebar-width);
            transition: all 0.3s ease;
            min-height: 100vh;
        }

        /* حالت جمع شده در دسکتاپ */
        .sidebar-collapsed .admin-sidebar {
            width: var(--sidebar-collapsed);
        }

        .sidebar-collapsed .admin-main {
            margin-right: var(--sidebar-collapsed);
        }

        /* لینک‌های سایدبار */
        .sidebar-link {
            display: flex;
            align-items: center;
            padding: 0.75rem 1rem;
            color: rgba(209, 213, 219, 0.9);
            border-radius: 0.5rem;
            margin: 0.25rem 0.5rem;
            transition: all 0.2s;
            white-space: nowrap;
            overflow: hidden;
            position: relative;
            text-decoration: none;
            cursor: pointer;
        }

            .sidebar-link:hover {
                background-color: rgba(255, 255, 255, 0.1);
                color: white;
            }

            .sidebar-link.active {
                background-color: rgba(37, 99, 235, 0.2);
                color: white;
                border-right: 3px solid #3b82f6;
            }

            .sidebar-link i {
                min-width: 20px;
                text-align: center;
                font-size: 1.125rem;
            }

            .sidebar-link span {
                margin-right: 0.75rem;
                transition: all 0.3s;
                font-size: 0.875rem;
            }

        /* حالت جمع شده */
        .sidebar-collapsed .sidebar-link span {
            opacity: 0;
            width: 0;
            margin: 0;
        }

        .sidebar-collapsed .sidebar-link {
            justify-content: center;
            padding: 0.75rem;
        }

        /* Overlay برای موبایل */
        .mobile-overlay {
            display: none;
            position: fixed;
            top: 0;
            right: 0;
            left: 0;
            bottom: 0;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 90;
        }

        /* تبلت و موبایل (کمتر از 992px) */
        @@media (max-width: 992px) {
            .admin-sidebar {
                transform: translateX(100%);
                width: 280px;
                transition: transform 0.3s ease;
            }

            .admin-main {
                margin-right: 0 !important;
            }

            .mobile-menu-btn {
                display: block !important;
            }

            .desktop-toggle-btn {
                display: none !important;
            }
            /* حالت باز در موبایل */
            .sidebar-open .admin-sidebar {
                transform: translateX(0);
                box-shadow: -2px 0 20px rgba(0, 0, 0, 0.25);
            }

            .sidebar-open .mobile-overlay {
                display: block;
                animation: fadeIn 0.3s ease;
            }
        }

        /* دسکتاپ متوسط (1200px-992px) */
        @@media (max-width: 1200px) and (min-width: 992px) {
            .admin-sidebar {
                width: var(--sidebar-collapsed);
            }

            .admin-main {
                margin-right: var(--sidebar-collapsed);
            }

            .sidebar-link span {
                display: none;
            }

            .sidebar-link {
                justify-content: center;
                padding: 0.75rem;
            }
        }

        /* انیمیشن‌ها */
        @@keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        /* استایل‌های دکمه‌ها */
      /*   .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 0.5rem 1rem;
            border-radius: 0.5rem;
            cursor: pointer;
            transition: all 0.2s;
            border: none;
            background: none;
        } */

        /* هدر */
        header {
            background-color: white;
            box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
            border-bottom: 1px solid #e5e7eb;
            position: sticky;
            top: 0;
            z-index: 50;
        }

        /* اسکرول بار سفارشی */
        .admin-sidebar::-webkit-scrollbar {
            width: 4px;
        }

        .admin-sidebar::-webkit-scrollbar-track {
            background: rgba(255, 255, 255, 0.1);
        }

        .admin-sidebar::-webkit-scrollbar-thumb {
            background: rgba(255, 255, 255, 0.3);
            border-radius: 2px;
        }
        /* استایل خاص برای دکمه مدیریت مالی */
        .btn-money {
            background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
            color: white !important;
            border: 1px solid #16a34a;
            box-shadow: 0 2px 4px rgba(34, 197, 94, 0.2);
            transition: all 0.3s ease;
        }

            .btn-money:hover {
                background: linear-gradient(135deg, #16a34a 0%, #15803d 100%);
                transform: translateY(-1px);
                box-shadow: 0 4px 8px rgba(34, 197, 94, 0.3);
            }

            .btn-money:active {
                transform: translateY(0);
            }

            .btn-money i {
                color: white !important;
            }
    </style>
    <script>
        // مدیریت سایدبار ریسپانسیو
        document.addEventListener('DOMContentLoaded', function() {
            const mobileMenuBtn = document.getElementById('mobileMenuToggle');
            const sidebarToggle = document.getElementById('sidebarToggle');
            const mobileOverlay = document.getElementById('mobileOverlay');
            const body = document.body;

            // تابع toggle برای دسکتاپ
            function toggleDesktopSidebar() {
                body.classList.toggle('sidebar-collapsed');
                localStorage.setItem('sidebarCollapsed', body.classList.contains('sidebar-collapsed'));
            }

            // تابع toggle برای موبایل
            function toggleMobileSidebar() {
                body.classList.toggle('sidebar-open');
                if (body.classList.contains('sidebar-open')) {
                    document.body.style.overflow = 'hidden';
                } else {
                    document.body.style.overflow = '';
                }
            }

            // تابع بستن سایدبار موبایل
            function closeMobileSidebar() {
                body.classList.remove('sidebar-open');
                document.body.style.overflow = '';
            }

            // رویدادها
            if (mobileMenuBtn) {
                mobileMenuBtn.addEventListener('click', toggleMobileSidebar);
            }

            if (sidebarToggle) {
                sidebarToggle.addEventListener('click', function() {
                    if (window.innerWidth >= 992) {
                        toggleDesktopSidebar();
                    } else {
                        toggleMobileSidebar();
                    }
                });
            }

            if (mobileOverlay) {
                mobileOverlay.addEventListener('click', closeMobileSidebar);
            }

            // بارگذاری وضعیت ذخیره شده
            const savedState = localStorage.getItem('sidebarCollapsed');
            if (savedState === 'true' && window.innerWidth >= 992) {
                body.classList.add('sidebar-collapsed');
            }

            // تنظیم اولیه برای تبلت
            if (window.innerWidth < 1200 && window.innerWidth >= 992) {
                body.classList.add('sidebar-collapsed');
            }

            // مدیریت ریسایز
            window.addEventListener('resize', function() {
                if (window.innerWidth >= 992) {
                    // بستن منوی موبایل در دسکتاپ
                    closeMobileSidebar();
                    // نمایش دکمه دسکتاپ
                    document.querySelector('.desktop-toggle-btn').style.display = 'flex';
                    document.querySelector('.mobile-menu-btn').style.display = 'none';
                } else {
                    // نمایش دکمه موبایل
                    document.querySelector('.desktop-toggle-btn').style.display = 'none';
                    document.querySelector('.mobile-menu-btn').style.display = 'flex';
                    // باز کردن سایدبار اگر قبلاً جمع شده بود
                    body.classList.remove('sidebar-collapsed');
                }

                // تنظیم برای تبلت
                if (window.innerWidth < 1200 && window.innerWidth >= 992) {
                    body.classList.add('sidebar-collapsed');
                }
            });

            // تنظیم اولیه نمایش دکمه‌ها
            if (window.innerWidth >= 992) {
                document.querySelector('.desktop-toggle-btn').style.display = 'flex';
                document.querySelector('.mobile-menu-btn').style.display = 'none';
            } else {
                document.querySelector('.desktop-toggle-btn').style.display = 'none';
                document.querySelector('.mobile-menu-btn').style.display = 'flex';
            }

            // بستن سایدبار با کلیک روی لینک‌ها در موبایل
            document.querySelectorAll('.sidebar-link').forEach(link => {
                link.addEventListener('click', function() {
                    if (window.innerWidth < 992) {
                        closeMobileSidebar();
                    }
                });
            });
        });
    </script>
    
    @*   <script>

                // توابع مدیریت پنل ادمین
        function toggleSidebar() {
            document.body.classList.toggle('sidebar-collapsed');
        }

        function toggleMobileSidebar() {
            const body = document.body;
            body.classList.toggle('sidebar-mobile-open');

            // جلوگیری از اسکرول بدنه هنگام باز بودن سایدبار
            if (body.classList.contains('sidebar-mobile-open')) {
                document.body.style.overflow = 'hidden';
            } else {
                document.body.style.overflow = 'auto';
            }
        }

        function closeMobileSidebar() {
            document.body.classList.remove('sidebar-mobile-open');
            document.body.style.overflow = 'auto';
        }

        function toggleProfileDropdown() {
            const dropdown = document.getElementById('profileDropdown');
            dropdown.classList.toggle('hidden');
        }

        // مدیریت منوی سایدبار
        document.getElementById('sidebarToggle').addEventListener('click', function(e) {
            e.stopPropagation();
            if (window.innerWidth < 768) {
                toggleMobileSidebar();
            } else {
                toggleSidebar();
            }
        });

        // بستن سایدبار هنگام کلیک روی overlay
        document.getElementById('sidebarOverlay')?.addEventListener('click', function(e) {
            e.stopPropagation();
            closeMobileSidebar();
        });

        // بستن سایدبار هنگام کلیک روی لینک‌های سایدبار در موبایل
        document.querySelectorAll('.sidebar-link').forEach(link => {
            link.addEventListener('click', function(e) {
                if (window.innerWidth < 768) {
                    closeMobileSidebar();
                }
            });
        });

        // بستن سایدبار موبایل هنگام تغییر سایز صفحه به بزرگتر از 768px
        window.addEventListener('resize', function() {
            if (window.innerWidth >= 768) {
                closeMobileSidebar();
            }
        });

        // جلوگیری از بستن سایدبار هنگام کلیک روی خود سایدبار
        document.querySelector('.admin-sidebar')?.addEventListener('click', function(e) {
            e.stopPropagation();
        });

        // بستن منوها هنگام کلیک خارج
        document.addEventListener('click', function(event) {
            const profileDropdown = document.getElementById('profileDropdown');
            const profileButton = event.target.closest('button[onclick="toggleProfileDropdown()"]');

            if (profileDropdown && !profileDropdown.contains(event.target) && !profileButton) {
                profileDropdown.classList.add('hidden');
            }
        });

        // بقیه کدهای JavaScript بدون تغییر باقی می‌مانند...
    </script> *@
</head>
<body class="admin-layout">
    <!-- Header -->
    <header class="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-40">
        <div class="max-w-full mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <!-- Logo and Toggle -->
                <div class="flex items-center">
                    <button id="sidebarToggle" class="p-2 rounded-lg text-gray-400 hover:text-gray-600 hover:bg-gray-100 transition-colors mr-2">
                        <i class="fas fa-bars"></i>
                    </button>
                    <a href="/">
                        <div class="flex items-center space-x-2 space-x-reverse">
                            <div class="w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center">
                                <span class="text-white font-bold text-lg">د</span>
                            </div>
                            <span class="text-xl font-bold text-gray-900">پلتفرم دنگ</span>
                        </div>
                    </a>
                </div>

                <!-- User Actions -->
                <div class="flex items-center space-x-4 space-x-reverse">
                    <!-- Notifications -->
                    <a href="/Notifications" class="relative p-2 text-gray-400 hover:text-gray-600 transition-colors">
                        <i class="fas fa-bell"></i>
                        <span class="badge badge-danger absolute -top-1 -left-1 min-w-[1.25rem] h-5 flex items-center justify-center">
                            3
                        </span>
                    </a>

                    <!-- Profile Dropdown -->
                    <div class="relative">
                        <button onclick="toggleProfileDropdown()"
                                class="flex items-center space-x-2 space-x-reverse p-2 rounded-lg hover:bg-gray-100 transition-colors">
                            <div class="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center">
                                <i class="fas fa-user text-blue-600"></i>
                            </div>
                            <span class="hidden sm:block font-medium text-gray-700">
                                ادمین
                            </span>
                        </button>

                        <!-- Dropdown Menu -->
                        <div id="profileDropdown" class="hidden absolute left-0 mt-2 w-48 bg-white rounded-lg shadow-lg border border-gray-200 py-1 z-50">
                            <a href="/Admin/Dashboard"
                               class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 transition-colors">
                                <i class="fas fa-tachometer-alt ml-3"></i>
                                داشبورد
                            </a>
                            <a href="/Profile"
                               class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 transition-colors">
                                <i class="fas fa-user ml-3"></i>
                                پروفایل
                            </a>
                            <form asp-page="/Logout" method="post" class="w-full">
                                <button type="submit"
                                        class="flex items-center w-full px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 transition-colors">
                                    <i class="fas fa-sign-out-alt ml-3"></i>
                                    خروج
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </header>

  @*   <div id="sidebarOverlay" class="sidebar-overlay" onclick="closeMobileSidebar()"></div>

 *@
    <div class="flex">
        <!-- Sidebar -->
        <div class="admin-sidebar fixed h-full z-30">
            <div class="flex flex-col h-full">
                <!-- Sidebar Header -->
                <div class="p-4 border-b border-blue-700">
                    <div class="flex items-center justify-between">
                        <div class="flex items-center space-x-2 space-x-reverse">
                            <div class="w-8 h-8 bg-white rounded-lg flex items-center justify-center">
                                <span class="text-blue-600 font-bold text-sm">د</span>
                            </div>
                            <span class="text-white font-bold">پنل مدیریت</span>
                        </div>
                    </div>
                </div>

                <!-- Sidebar Menu -->
                <partial name="~/Pages/Shared/_adminSidebar.cshtml" />
            </div>
        </div>

        <!-- Main Content -->
        <div class="admin-main flex-1">
            <div class="admin-content">
                <!-- Page Header -->
                <div class="bg-white border-b border-gray-200">
                    <div class="max-w-full mx-auto px-4 sm:px-6 lg:px-8 py-6">
                        <div class="flex justify-between items-center">
                            <div>
                                <h1 class="text-2xl font-bold text-gray-900">مدیریت کاربران</h1>
                                <p class="mt-1 text-sm text-gray-600">مدیریت و مشاهده کاربران پلتفرم</p>
                            </div>
                            <div class="flex items-center space-x-3 space-x-reverse">
                             @*    <button onclick="exportUsers()" class="btn btn-outline">
                                    <i class="fas fa-download ml-2"></i>
                                    خروجی Excel
                                </button> *@
                                <a href="/Admin/Users/Add" class="btn btn-primary">
                                    <i class="fas fa-user-plus ml-2"></i>
                                    افزودن کاربر جدید
                                </a>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Main Content Area -->
                <div class="max-w-full mx-auto px-4 sm:px-6 lg:px-8 py-6">
                    <!-- Users List -->
                    <div class="card">
                        <div class="p-6">
                            <form method="get">
                                <div class="flex flex-col lg:flex-row lg:justify-between lg:items-center mb-6 space-y-4 lg:space-y-0">
                                    <h2 class="text-lg font-semibold text-gray-900">لیست کاربران</h2>

                                    <!-- Search and Filters -->
                                    <div class="flex flex-col sm:flex-row items-start sm:items-center space-y-3 sm:space-y-0 sm:space-x-3 space-x-reverse">
                                        <div class="relative w-full sm:w-64">
                                            <input type="text"
                                                   id="searchInput"
                                                   asp-for="@Model.FilterParams.Search"
                                                   placeholder="جستجو بر اساس شماره تلفن..."
                                                   class="input pl-10">
                                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                                <i class="fas fa-search text-gray-400"></i>
                                            </div>
                                        </div>


                                        <select id="statusFilter" asp-for="@Model.FilterParams.IsActive" class="input w-full sm:w-auto">
                                            <option value="">همه وضعیت‌ها</option>
                                            <option value="true">فعال</option>
                                            <option value="false">غیرفعال</option>
                                        </select>

                                 @*        <select id="userStatusFilter" asp-for="@Model.FilterParams." class="input w-full sm:w-auto">
                                            <option value="">همه کاربران</option>
                                            <option value="IsConfirmed">تایید شده</option>
                                            <option value="AwaitingConfirmation">در انتظار تایید</option>
                                            <option value="NotConfirmed">تایید نشده</option>
                                            <option value="WrongInformation">اطلاعات اشتباه</option>
                                        </select> *@

                                        <button @* onclick="applyFilters()" *@ class="btn btn-primary w-full sm:w-auto">
                                            <i class="fas fa-filter ml-2"></i>
                                            اعمال فیلتر
                                        </button>
                                    </div>
                                </div>
                            </form>
                            <!-- Statistics Cards -->
                            <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-6">
                                <div class="card card-hover">
                                    <div class="p-4">
                                        <div class="flex items-center justify-between">
                                            <div>
                                                <p class="text-sm font-medium text-gray-600">کل کاربران</p>
                                                <p class="text-2xl font-bold text-gray-900">@Model.Users.EntityCount</p>
                                            </div>
                                            <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
                                                <i class="fas fa-users text-blue-600"></i>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="card card-hover">
                                    <div class="p-4">
                                        <div class="flex items-center justify-between">
                                            <div>
                                                <p class="text-sm font-medium text-gray-600">کاربران فعال</p>
                                                <p class="text-2xl font-bold text-gray-900">
                                                    @(Model.Users.Data.Count(user => user.IsActive == true))
                                                </p>
                                            </div>
                                            <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center">
                                                <i class="fas fa-user-check text-green-600"></i>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="card card-hover">
                                    <div class="p-4">
                                        <div class="flex items-center justify-between">
                                            <div>
                                                <p class="text-sm font-medium text-gray-600">در انتظار تایید</p>
                                                <p class="text-2xl font-bold text-gray-900">
                                                    @(Model.Users.Data.Count(user => user.UserDocument?.Status == UserDocumentStatus.AwaitingConfirmation))
                                                </p>
                                            </div>
                                            <div class="w-12 h-12 bg-yellow-100 rounded-lg flex items-center justify-center">
                                                <i class="fas fa-user-clock text-yellow-600"></i>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="card card-hover">
                                    <div class="p-4">
                                        <div class="flex items-center justify-between">
                                            <div>
                                                <p class="text-sm font-medium text-gray-600">کاربران مسدود</p>

                                                <p class="text-2xl font-bold text-gray-900">
                                                    @(Model.Users.Data.Count(user => user.UserBlocks?.Any(i => i.UserId == user.Id && i.BlockToDate > DateTime.Now) == true))
                                                </p>
                                            </div>
                                            <div class="w-12 h-12 bg-red-100 rounded-lg flex items-center justify-center">
                                                <i class="fas fa-user-slash text-red-600"></i>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Users Table -->
                            <div class="table-responsive">
                                <table class="table">
                                    <thead>
                                        <tr>
                                            <th>#</th>
                                            <th>کاربر</th>
                                            <th>شماره تلفن</th>
                                            @* <th>کد ملی</th> *@
                                            <th>نقش</th>
                                            <th>وضعیت</th>
                                            <th>فعال</th>
                                            <th>تاریخ ثبت‌نام</th>
                                            <th>عملیات</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @{
                                            int number = 1;
                                            foreach (var item in Model.Users.Data)
                                            {
                                                <tr>
                                                    <td>@(number++)</td>
                                                    <td>
                                                        <div class="flex items-center space-x-3 space-x-reverse">
                                                            <img src=@Directories.GetUserImageAccountPath(item.ImageName)
                                                                 alt="@item.FirstName @item.LastName"
                                                                 class="user-avatar">
                                                            <div>
                                                                    <div class="font-medium text-gray-900">
                                                                        @(item.FirstName + " " + item.LastName ?? "کاربر ثبت نام انجام نداده است")
                                                                    </div>
                                                                @*<div class="text-sm text-gray-500">شناسه: USR-001</div>*@
                                                            </div>
                                                        </div>
                                                    </td>
                                                    <td>
                                                        <div class="flex items-center">
                                                            <span class="text-gray-700">@item.PhoneNumber</span>
                                                            <span class="status-dot status-active"></span>
                                                        </div>
                                                    </td>
                                                    @*<td>@item.NationalityCode</td>*@
                                                    <td>
                                                        <span class="badge badge-primary">@(item.UserRole?.RoleName ?? "Guest")</span>
                                                    </td>
                                                    <td>
                                                        <span class="badge badge-success">
                                                            @(item.UserDocument?.Status.Map() ?? UserDocumentStatus.NotConfirmed.Map())

                                                        </span>
                                                    </td>
                                                    <td>
                                                        <span class="badge badge-success">
                                                            @(item.IsActive ? "بله" : "خیر")
                                                        </span>
                                                    </td>
                                                    <td>@item.CreationDate.ToPersianDate()</td>
                                                    <td>
                                                        <div class="flex items-center space-x-2 space-x-reverse">
                                                            @*<a asp-page="Edit" asp-route-userId="@item.Id" class="btn btn-outline btn-sm" title="ویرایش">
                                                                <i class="fas fa-edit"></i>
                                                            </a>*@
                                                            <button class="btn btn-outline btn-sm" title="مشاهده جزئیات">
                                                                <i class="fas fa-eye"></i>
                                                            </button>
                                                            @*<button class="btn btn-warning btn-sm" title="مسدود کردن">
                                                                <i class="fas fa-lock"></i>
                                                            </button>*@
                                                            <!-- دکمه حذف -->
                                                            <button onclick="deleteUser('@item.Id')" class="btn btn-danger btn-sm" title="حذف">
                                                                <i class="fas fa-trash"></i>
                                                            </button>
                                                            <button onclick="location.href='/Admin/Order/Add?userId=@item.Id'"
                                                                    class="btn btn-sm px-2 bg-green-500 hover:bg-green-600 text-white border-green-600"
                                                                    title="مدیریت مالی">
                                                                <i class="fas fa-money-bill-wave"></i>
                                                            </button>
                                                        </div>
                                                    </td>
                                                </tr>
                                            }
                                        }
                                        @Html.AntiForgeryToken()
                                        @* 
                                        <tr>
                                            <td>2</td>
                                            <td>
                                                <div class="flex items-center space-x-3 space-x-reverse">
                                                    <img src="data:image/svg+xml;base64,"
                                                         alt="فاطمه کریمی"
                                                         class="user-avatar">
                                                    <div>
                                                        <div class="font-medium text-gray-900">فاطمه کریمی</div>
                                                        <div class="text-sm text-gray-500">شناسه: USR-002</div>
                                                    </div>
                                                </div>
                                            </td>
                                            <td>
                                                <div class="flex items-center">
                                                    <span class="text-gray-700">۰۹۳۵۱۲۳۴۵۶۷</span>
                                                    <span class="status-dot status-active"></span>
                                                </div>
                                            </td>
                                            <td>۰۰۲۳۴۵۶۷۸۹</td>
                                            <td>
                                                <span class="badge badge-info">پروژه‌گذار</span>
                                            </td>
                                            <td>
                                                <span class="badge badge-warning">در انتظار تایید</span>
                                            </td>
                                            <td>
                                                <span class="badge badge-success">بله</span>
                                            </td>
                                            <td>۱۴۰۲/۰۵/۱۰</td>
                                            <td>
                                                <div class="flex items-center space-x-2 space-x-reverse">
                                                    <a href="/Admin/Users/Edit/2" class="btn btn-outline btn-sm" title="ویرایش">
                                                        <i class="fas fa-edit"></i>
                                                    </a>
                                                    <button class="btn btn-outline btn-sm" title="مشاهده جزئیات">
                                                        <i class="fas fa-eye"></i>
                                                    </button>
                                                    <button class="btn btn-success btn-sm" title="تایید کاربر">
                                                        <i class="fas fa-check"></i>
                                                    </button>
                                                    <button class="btn btn-danger btn-sm" title="حذف">
                                                        <i class="fas fa-trash"></i>
                                                    </button>
                                                </div>
                                            </td>
                                        </tr>
                                        <tr>
                                            <td>3</td>
                                            <td>
                                                <div class="flex items-center space-x-3 space-x-reverse">
                                                    <div class="user-avatar bg-gray-200 flex items-center justify-center">
                                                        <i class="fas fa-user text-gray-400"></i>
                                                    </div>
                                                    <div>
                                                        <div class="font-medium text-gray-900">رضا محمدی</div>
                                                        <div class="text-sm text-gray-500">شناسه: USR-003</div>
                                                    </div>
                                                </div>
                                            </td>
                                            <td>
                                                <div class="flex items-center">
                                                    <span class="text-gray-700">۰۹۱۸۱۲۳۴۵۶۷</span>
                                                    <span class="status-dot status-inactive"></span>
                                                </div>
                                            </td>
                                            <td>۰۰۳۴۵۶۷۸۹۰</td>
                                            <td>
                                                <span class="badge badge-primary">سرمایه‌گذار</span>
                                            </td>
                                            <td>
                                                <span class="badge badge-danger">مسدود شده</span>
                                            </td>
                                            <td>
                                                <span class="badge badge-danger">خیر</span>
                                            </td>
                                            <td>۱۴۰۲/۰۴/۲۸</td>
                                            <td>
                                                <div class="flex items-center space-x-2 space-x-reverse">
                                                    <a href="/Admin/Users/Edit/3" class="btn btn-outline btn-sm" title="ویرایش">
                                                        <i class="fas fa-edit"></i>
                                                    </a>
                                                    <button class="btn btn-outline btn-sm" title="مشاهده جزئیات">
                                                        <i class="fas fa-eye"></i>
                                                    </button>
                                                    <button class="btn btn-success btn-sm" title="رفع مسدودیت">
                                                        <i class="fas fa-unlock"></i>
                                                    </button>
                                                    <button class="btn btn-danger btn-sm" title="حذف">
                                                        <i class="fas fa-trash"></i>
                                                    </button>
                                                </div>
                                            </td>
                                        </tr> *@
                                    </tbody>
                                </table>
                            </div>

                            <!-- Pagination -->
                            <div class="flex flex-col sm:flex-row items-center justify-between mt-6 space-y-4 sm:space-y-0">
                                <div class="text-sm text-gray-700">
                                    نمایش
                                    <span id="currentRange">@((Model.Users.CurrentPage - 1) * Model.Users.Take + 1) - @Math.Min(Model.Users.CurrentPage * Model.Users.Take, Model.Users.EntityCount)</span>
                                    از
                                    <span id="totalCount">@Model.Users.EntityCount</span>
                                    کاربر
                                </div>
                                <div class="flex items-center space-x-2 space-x-reverse">
                                    <!-- دکمه صفحه قبلی -->
                                    <button onclick="loadPage(@(Model.Users.CurrentPage - 1))"
                                            class="btn btn-outline btn-sm @(Model.Users.CurrentPage <= 1 ? "opacity-50 cursor-not-allowed" : "")"
                                            @(Model.Users.CurrentPage <= 1 ? "disabled" : "")>
                                        <i class="fas fa-chevron-right"></i>
                                    </button>

                                    <!-- صفحات عددی -->
                                    @for (int i = Model.Users.StartPage; i <= Model.Users.EndPage; i++)
                                    {
                                        if (i == Model.Users.CurrentPage)
                                        {
                                            <button class="btn btn-primary btn-sm">@i</button>
                                        }
                                        else
                                        {
                                            <button onclick="loadPage(@i)" class="btn btn-outline btn-sm">@i</button>
                                        }
                                    }

                                    <!-- دکمه صفحه بعدی -->
                                    <button onclick="loadPage(@(Model.Users.CurrentPage + 1))"
                                            class="btn btn-outline btn-sm @(Model.Users.CurrentPage >= Model.Users.PageCount ? "opacity-50 cursor-not-allowed" : "")"
                                            @(Model.Users.CurrentPage >= Model.Users.PageCount ? "disabled" : "")>
                                        <i class="fas fa-chevron-left"></i>
                                    </button>
                                </div>
                            </div>

                            <!-- انتخاب تعداد آیتم در صفحه -->
                            <div class="flex items-center justify-between mt-4 pt-4 border-t border-gray-200">
                                <div class="flex items-center space-x-2 space-x-reverse">
                                    <span class="text-sm text-gray-600">تعداد در هر صفحه:</span>
                                    <select onchange="changePageSize(this.value)" class="input w-20 text-sm py-1" id="pageSizeSelect">
                                        <option value="8" selected="@(Model.Users.Take == 8)">8</option>
                                        <option value="16" selected="@(Model.Users.Take == 16)">16</option>
                                        <option value="32" selected="@(Model.Users.Take == 32)">32</option>
                                        <option value="50" selected="@(Model.Users.Take == 50)">50</option>
                                    </select>
                                </div>
                            </div>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- User Details Modal -->
    <div id="userDetailsModal" class="fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full z-50 hidden">
        <div class="relative top-20 mx-auto p-5 border w-full max-w-4xl shadow-lg rounded-lg bg-white">
            <div class="flex justify-between items-center pb-3 border-b">
                <h3 class="text-xl font-semibold text-gray-900">جزئیات کاربر</h3>
                <button onclick="closeUserDetailsModal()" class="text-gray-400 hover:text-gray-600">
                    <i class="fas fa-times"></i>
                </button>
            </div>

            <div class="mt-4">
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                    <!-- User Info -->
                    <div class="md:col-span-2 space-y-6">
                        <div class="card">
                            <div class="p-4 border-b border-gray-200">
                                <h4 class="text-lg font-semibold text-gray-900">اطلاعات شخصی</h4>
                            </div>
                            <div class="p-4">
                                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                    <div>
                                        <label class="form-label">نام</label>
                                        <p class="text-gray-900">محمد</p>
                                    </div>
                                    <div>
                                        <label class="form-label">نام خانوادگی</label>
                                        <p class="text-gray-900">احمدی</p>
                                    </div>
                                    <div>
                                        <label class="form-label">شماره تلفن</label>
                                        <p class="text-gray-900">۰۹۱۲۱۲۳۴۵۶۷</p>
                                    </div>
                                    <div>
                                        <label class="form-label">کد ملی</label>
                                        <p class="text-gray-900">۰۰۱۲۳۴۵۶۷۸</p>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="card">
                            <div class="p-4 border-b border-gray-200">
                                <h4 class="text-lg font-semibold text-gray-900">اطلاعات حساب</h4>
                            </div>
                            <div class="p-4">
                                <div class="space-y-3">
                                    <div class="flex justify-between items-center">
                                        <span class="text-gray-600">تاریخ ثبت‌نام:</span>
                                        <span class="text-gray-900">۱۴۰۲/۰۵/۱۵</span>
                                    </div>
                                    <div class="flex justify-between items-center">
                                        <span class="text-gray-600">آخرین ورود:</span>
                                        <span class="text-gray-900">۱۴۰۲/۰۶/۲۰ - ۱۴:۳۰</span>
                                    </div>
                                    <div class="flex justify-between items-center">
                                        <span class="text-gray-600">وضعیت:</span>
                                        <span class="badge badge-success">فعال</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Sidebar -->
                    @* <div class="space-y-6">
                        <div class="card">
                            <div class="p-4">
                                <h4 class="text-lg font-semibold text-gray-900 mb-4">عملیات سریع</h4>
                                <div class="space-y-2">
                                    <button class="btn btn-outline w-full text-right btn-sm">
                                        <i class="fas fa-edit ml-2"></i>
                                        ویرایش کاربر
                                    </button>
                                    <button class="btn btn-outline w-full text-right btn-sm">
                                        <i class="fas fa-lock ml-2"></i>
                                        مسدود کردن
                                    </button>
                                    <button class="btn btn-outline w-full text-right btn-sm">
                                        <i class="fas fa-envelope ml-2"></i>
                                        ارسال پیام
                                    </button>
                                </div>
                            </div>
                        </div>

                        <div class="card">
                            <div class="p-4">
                                <h4 class="text-lg font-semibold text-gray-900 mb-4">آمار کاربر</h4>
                                <div class="space-y-2 text-sm">
                                    <div class="flex justify-between">
                                        <span class="text-gray-600">تعداد پروژه‌ها:</span>
                                        <span class="text-gray-900">۱۲</span>
                                    </div>
                                    <div class="flex justify-between">
                                        <span class="text-gray-600">سرمایه‌گذاری‌ها:</span>
                                        <span class="text-gray-900">۲۴</span>
                                    </div>
                                    <div class="flex justify-between">
                                        <span class="text-gray-600">موجودی کیف پول:</span>
                                        <span class="text-gray-900">۵,۰۰۰,۰۰۰ ریال</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>*@
                </div>
            </div>
        </div>
    </div>

    <script>
        // توابع مدیریت پنل ادمین
        function toggleSidebar() {
            document.body.classList.toggle('sidebar-collapsed');
        }

        function toggleMobileSidebar() {
            document.body.classList.toggle('sidebar-mobile-open');
        }

        function toggleProfileDropdown() {
            const dropdown = document.getElementById('profileDropdown');
            dropdown.classList.toggle('hidden');
        }

        function applyFilters() {
            const search = document.getElementById('searchInput').value;
            const status = document.getElementById('statusFilter').value;
            const userStatus = document.getElementById('userStatusFilter').value;

            // شبیه‌سازی اعمال فیلتر
            console.log('اعمال فیلتر:', { search, status, userStatus });
            alert('فیلترها اعمال شدند');
        }

        function exportUsers() {
            alert('خروجی Excel در حال آماده‌سازی است...');
        }

        function showUserDetails() {
            document.getElementById('userDetailsModal').classList.remove('hidden');
        }

        function closeUserDetailsModal() {
            document.getElementById('userDetailsModal').classList.add('hidden');
        }

        // مدیریت منوی سایدبار
        document.getElementById('sidebarToggle').addEventListener('click', function() {
            if (window.innerWidth < 768) {
                toggleMobileSidebar();
            } else {
                toggleSidebar();
            }
        });

        // // اسکریپت حذف کاربر با SweetAlert
        //       async function deleteUser(userId) {
        //           // استفاده از SweetAlert برای تایید حذف
        //           const result = await Swal.fire({
        //               title: 'آیا از حذف این کاربر اطمینان دارید؟',
        //               text: "این عمل قابل بازگشت نیست!",
        //               icon: 'warning',
        //               showCancelButton: true,
        //               confirmButtonColor: '#d33',
        //               cancelButtonColor: '#3085d6',
        //               confirmButtonText: 'بله، حذف شود',
        //               cancelButtonText: 'لغو',
        //               customClass: {
        //                   confirmButton: 'btn btn-danger',
        //                   cancelButton: 'btn btn-secondary'
        //               }
        //           });

        //           if (result.isConfirmed) {
        //               try {
        //                   const formData = new FormData();
        //                   formData.append('userId', userId);

        //                   const response = await fetch('?handler=Delete', {
        //                       method: 'POST',
        //                       body: formData,
        //                       headers: {
        //                           'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]').value
        //                       }
        //                   });

        //                   if (response.ok) {
        //                       // نمایش پیام موفقیت با SweetAlert
        //                       await Swal.fire({
        //                           title: 'حذف شد!',
        //                           text: 'کاربر با موفقیت حذف شد.',
        //                           icon: 'success',
        //                           confirmButtonText: 'باشه',
        //                           confirmButtonColor: '#28a745'
        //                       });

        //                       // رفرش صفحه پس از حذف
        //                       location.reload();
        //                   } else {
        //                       await Swal.fire({
        //                           title: 'خطا!',
        //                           text: 'خطا در حذف کاربر',
        //                           icon: 'error',
        //                           confirmButtonText: 'باشه',
        //                           confirmButtonColor: '#dc3545'
        //                       });
        //                   }
        //               } catch (error) {
        //                   console.error('Error:', error);
        //                   await Swal.fire({
        //                       title: 'خطا!',
        //                       text: 'خطا در ارتباط با سرور',
        //                       icon: 'error',
        //                       confirmButtonText: 'باشه',
        //                       confirmButtonColor: '#dc3545'
        //                   });
        //               }
        //           }
        //       }

              // بقیه توابع بدون تغییر









                      // تابع اصلی برای لود صفحه با Ajax
        async function loadPage(pageId) {
            try {
                showLoading();

                const take = document.getElementById('pageSizeSelect')?.value || @Model.Users.Take;
                const phoneNumber = document.getElementById('searchInput')?.value || '';
                const isActive = document.getElementById('statusFilter')?.value || '';

                const params = new URLSearchParams({
                    pageId: pageId,
                    take: take,
                    phoneNumber: phoneNumber,
                    isActive: isActive
                });

                // اضافه کردن پارامترها به URL بدون رفرش صفحه
                const newUrl = `${window.location.pathname}?${params.toString()}`;
                window.history.pushState({}, '', newUrl);

                const response = await fetch(`?${params.toString()}`, {
                    method: 'GET',
                    headers: {
                        'X-Requested-With': 'XMLHttpRequest',
                        'Accept': 'text/html'
                    }
                });

                if (!response.ok) {
                    throw new Error('خطا در دریافت داده‌ها');
                }

                const html = await response.text();
                await updatePageContent(html);

            } catch (error) {
                console.error('Error:', error);
                await Swal.fire({
                    title: 'خطا!',
                    text: 'خطا در بارگذاری داده‌ها',
                    icon: 'error',
                    confirmButtonText: 'باشه'
                });
            } finally {
                hideLoading();
            }
        }

        // تغییر سایز صفحه
        async function changePageSize(pageSize) {
            await loadPage(1); // بازگشت به صفحه اول با سایز جدید
        }

        // اعمال فیلترها
        async function applyFilters() {
            await loadPage(1); // بازگشت به صفحه اول با فیلترهای جدید
        }

        // آپدیت محتوای صفحه
        async function updatePageContent(html) {
            // ایجاد یک المان موقت برای پارس کردن HTML
            const tempDiv = document.createElement('div');
            tempDiv.innerHTML = html;

            // آپدیت کارت‌های آمار
            const newStats = tempDiv.querySelector('.grid.grid-cols-1.md\\:grid-cols-4.gap-4.mb-6');
            if (newStats) {
                const currentStats = document.querySelector('.grid.grid-cols-1.md\\:grid-cols-4.gap-4.mb-6');
                currentStats.innerHTML = newStats.innerHTML;
            }

            // آپدیت جدول کاربران
            const newTableBody = tempDiv.querySelector('tbody');
            if (newTableBody) {
                document.querySelector('tbody').innerHTML = newTableBody.innerHTML;
            }

            // آپدیت اطلاعات صفحه‌بندی
            const newPaginationInfo = tempDiv.querySelector('.flex.flex-col.sm\\:flex-row.items-center.justify-between.mt-6');
            if (newPaginationInfo) {
                const currentPagination = document.querySelector('.flex.flex-col.sm\\:flex-row.items-center.justify-between.mt-6');
                currentPagination.outerHTML = newPaginationInfo.outerHTML;
            }

            // آپدیت انتخاب سایز صفحه
            const newPageSizeSelect = tempDiv.querySelector('#pageSizeSelect');
            if (newPageSizeSelect) {
                const currentSelect = document.querySelector('#pageSizeSelect');
                currentSelect.innerHTML = newPageSizeSelect.innerHTML;
                // اضافه کردن event listener جدید
                currentSelect.onchange = function() { changePageSize(this.value); };
            }

            // ری‌اتچ event listeners برای دکمه‌های حذف
            reattachEventListeners();
        }

        // ری‌اتچ event listeners
        function reattachEventListeners() {
            // حذف event listeners قبلی و اضافه کردن جدید
            document.querySelectorAll('button[onclick^="deleteUser"]').forEach(button => {
                const oldOnclick = button.getAttribute('onclick');
                const userId = oldOnclick.match(/'([^']+)'/)[1];
                button.removeAttribute('onclick');
                button.addEventListener('click', () => deleteUser(userId));
            });
        }

        // نمایش loading
        function showLoading() {
            const existingLoader = document.getElementById('tableLoader');
            if (!existingLoader) {
                const loader = document.createElement('div');
                loader.id = 'tableLoader';
                loader.className = 'fixed inset-0 bg-gray-600 bg-opacity-50 flex items-center justify-center z-50';
                loader.innerHTML = `
                    <div class="bg-white p-6 rounded-lg shadow-lg">
                        <div class="flex items-center space-x-3 space-x-reverse">
                            <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600"></div>
                            <span class="text-gray-700">در حال بارگذاری...</span>
                        </div>
                    </div>
                `;
                document.body.appendChild(loader);
            }
        }

        // مخفی کردن loading
        function hideLoading() {
            const loader = document.getElementById('tableLoader');
            if (loader) {
                loader.remove();
            }
        }

        // هندل رویداد popstate برای دکمه‌های قبلی/بعدی مرورگر
        window.addEventListener('popstate', function(event) {
            loadPageFromURL();
        });

        // لود صفحه از URL
        function loadPageFromURL() {
            const urlParams = new URLSearchParams(window.location.search);
            const pageId = urlParams.get('pageId') || 1;
            loadPage(parseInt(pageId));
        }

        // اسکریپت حذف کاربر با SweetAlert (آپدیت شده)
               // اسکریپت حذف کاربر با SweetAlert
            // اسکریپت حذف کاربر با بررسی دقیق پاسخ
        async function deleteUser(userId) {
            const result = await Swal.fire({
                title: 'آیا از حذف این کاربر اطمینان دارید؟',
                text: "این عمل قابل بازگشت نیست!",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#d33',
                cancelButtonColor: '#3085d6',
                confirmButtonText: 'بله، حذف شود',
                cancelButtonText: 'لغو'
            });

            if (result.isConfirmed) {
                try {
                    Swal.fire({
                        title: 'در حال حذف...',
                        text: 'لطفا منتظر بمانید',
                        allowOutsideClick: false,
                        didOpen: () => {
                            Swal.showLoading();
                        }
                    });

                    // روش 1: استفاده از FormData
                    const formData = new FormData();
                    formData.append('userId', userId);

                    // روش 2: استفاده از URLSearchParams
                    // const params = new URLSearchParams();
                    // params.append('userId', userId);

                    const token = document.querySelector('input[name="__RequestVerificationToken"]').value;

                    const response = await fetch(`?handler=Delete`, {
                        method: 'POST',
                        body: formData,
                        headers: {
                            // 'Content-Type': 'application/x-www-form-urlencoded', // اگر از URLSearchParams استفاده می‌کنید
                            'RequestVerificationToken': token,
                            'X-Requested-With': 'XMLHttpRequest' // برای تشخیص درخواست Ajax
                        }
                    });

                    const responseText = await response.text();

                    // ایجاد یک DOM موقت برای تحلیل HTML پاسخ
                    const parser = new DOMParser();
                    const doc = parser.parseFromString(responseText, 'text/html');

                    // بررسی وجود پیام خطا در پاسخ
                    const errorElements = doc.querySelectorAll('[id*="Error"], .alert-danger, .text-danger');
                    const successElements = doc.querySelectorAll('[id*="Success"], .alert-success, .text-success');

                    // بررسی محتوای صفحه برای پیام‌ها
                    let hasError = false;
                    let errorMessage = '';
                    let successMessage = '';

                    // اگر عنصر خطا پیدا شد
                    if (errorElements.length > 0) {
                        hasError = true;
                        errorElements.forEach(el => {
                            if (el.textContent.trim()) {
                                errorMessage += el.textContent.trim() + '\n';
                            }
                        });
                    }

                    // اگر عنصر موفقیت پیدا شد
                    if (successElements.length > 0) {
                        successElements.forEach(el => {
                            if (el.textContent.trim()) {
                                successMessage += el.textContent.trim() + '\n';
                            }
                        });
                    }

                    // اگر در بدنه صفحه متن خطا وجود دارد (مثلاً در تگ script)
                    if (responseText.includes('TempData["Error"]') && responseText.includes('خطا')) {
                        // استخراج پیام خطا از کد JavaScript
                        const errorMatch = responseText.match(/TempData\["Error"\]\s*[=:]\s*['"]([^'"]+)['"]/);
                        if (errorMatch && errorMatch[1]) {
                            hasError = true;
                            errorMessage = decodeURIComponent(errorMatch[1]);
                        }
                    }

                    // بررسی وضعیت پاسخ
                    if (!response.ok) {
                        hasError = true;
                        errorMessage = `خطای سرور: ${response.status} ${response.statusText}`;
                    }

                    Swal.close();

                    // if (hasError) {
                    //     await Swal.fire({
                    //         title: 'خطا در حذف',
                    //         text: errorMessage || 'خطای نامشخص در حذف کاربر',
                    //         icon: 'error',
                    //         confirmButtonText: 'باشه'
                    //     });
                    // } else {
                    //     await Swal.fire({
                    //         title: 'موفقیت',
                    //         text: successMessage || 'کاربر با موفقیت حذف شد',
                    //         icon: 'success',
                    //         confirmButtonText: 'باشه'
                    //     });

                        // رفرش صفحه برای نمایش تغییرات
                        location.reload();
                    // }

                } catch (error) {
                    console.error('Error:', error);
                    Swal.close();

                    await Swal.fire({
                        title: 'خطای شبکه',
                        text: 'خطا در ارتباط با سرور. لطفا اتصال اینترنت را بررسی کنید.',
                        icon: 'error',
                        confirmButtonText: 'باشه'
                    });
                }
            }
        }

        // بستن منوها هنگام کلیک خارج
        document.addEventListener('click', function(event) {
            const profileDropdown = document.getElementById('profileDropdown');
            const profileButton = event.target.closest('button[onclick="toggleProfileDropdown()"]');

            if (profileDropdown && !profileDropdown.contains(event.target) && !profileButton) {
                profileDropdown.classList.add('hidden');
            }

            // بستن سایدبار موبایل هنگام کلیک خارج
            if (window.innerWidth < 768 &&
                !event.target.closest('.admin-sidebar') &&
                !event.target.closest('#sidebarToggle')) {
                document.body.classList.remove('sidebar-mobile-open');
            }
        });

        // جستجوی لحظه‌ای
        document.getElementById('searchInput').addEventListener('input', function() {
            const searchTerm = this.value.toLowerCase();
            // شبیه‌سازی جستجوی لحظه‌ای
            if (searchTerm.length >= 3) {
                console.log('جستجو برای:', searchTerm);
            }
        });
        

        // مدیریت کلیک روی دکمه مشاهده جزئیات
        document.querySelectorAll('button[title="مشاهده جزئیات"]').forEach(button => {
            button.addEventListener('click', showUserDetails);
        });

        //         // اسکریپت حذف کاربر
        // async function deleteUser(userId) {
        //     if (!confirm('آیا از حذف این کاربر اطمینان دارید؟')) {
        //         return;
        //     }

        //     try {
        //         const formData = new FormData();
        //         formData.append('userId', userId);

        //         const response = await fetch('?handler=Delete', {
        //             method: 'POST',
        //             body: formData,
        //             headers: {
        //                 'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]').value
        //             }
        //         });

        //         if (response.ok) {
        //             location.reload(); // رفرش صفحه پس از حذف
        //         } else {
        //             alert('خطا در حذف کاربر');
        //         }
        //     } catch (error) {
        //         console.error('Error:', error);
        //         alert('خطا در حذف کاربر');
        //     }
        // }
    </script>
    <script src="~/Alert.js"></script>

    <script>
        // نمایش خودکار پیام‌های TempData
        function showTempDataMessages() {
        // موفقیت
        @if (TempData["Success"] != null)
        {
                    <text>
                        Alert.success(decodePersianText('@TempData["Success"]'));
                    </text>
        }

        // خطا
        @if (TempData["Error"] != null)
        {
                    <text>
                        const errorMsg = decodePersianText('@TempData["Error"]');
                        if (errorMsg.includes('404') || errorMsg.includes('یافت نشد')) {
                        Alert.notFound(errorMsg);
                        } else if (errorMsg.includes('500') || errorMsg.includes('سرور')) {
                        Alert.serverError(errorMsg);
                        } else if (errorMsg.includes('400') || errorMsg.includes('نامعتبر')) {
                        Alert.badRequest(errorMsg);
                        } else if (errorMsg.includes('401') || errorMsg.includes('دسترسی')) {
                        Alert.unauthorized(errorMsg);
                        } else {
                        Alert.error(errorMsg);
                        }
                    </text>
        }

        // هشدار
        @if (TempData["Warning"] != null)
        {
                    <text>
                        const warningMsg = decodePersianText('@TempData["Warning"]');
                        if (warningMsg.includes('اعتبارسنجی') || warningMsg.includes('بررسی')) {
                        Alert.validationWarning(warningMsg);
                        } else if (warningMsg.includes('مالی') || warningMsg.includes('موجودی')) {
                        Alert.financialWarning(warningMsg);
                        } else if (warningMsg.includes('امنیتی') || warningMsg.includes('غیرامن')) {
                        Alert.securityWarning(warningMsg);
                        } else {
                        Alert.warning(warningMsg);
                        }
                    </text>
        }

        // اطلاعیه
        @if (TempData["Info"] != null)
        {
                    <text>
                        const infoMsg = decodePersianText('@TempData["Info"]');
                        if (infoMsg.includes('سیستم') || infoMsg.includes('بروزرسانی')) {
                        Alert.systemInfo(infoMsg);
                        } else if (infoMsg.includes('مالی') || infoMsg.includes('تراکنش')) {
                        Alert.financialInfo(infoMsg);
                        } else {
                        Alert.info(infoMsg);
                        }
                    </text>
        }
        }

        // نمایش پیام‌ها پس از لود صفحه
        document.addEventListener('DOMContentLoaded', function() {
            showTempDataMessages();
        });
    </script>

</body>
</html>